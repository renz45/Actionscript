package com.rensel.bitmapUtils
{
	import flash.events.EventDispatcher;
	import flash.events.IEventDispatcher;
	
	public class SplitBmp extends EventDispatcher
	{
		public function SplitBmp(target:IEventDispatcher=null)
		{
			super(target);
		}
	}
}